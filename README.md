# Mini Kanban API

Простое REST API для управления досками, задачами и карточками в стиле Kanban.  
Версия с **Docker-контейнеризацией**, **PostgreSQL**.

---

## 🚀 Запуск через Docker Compose

Проект запускается в двух контейнерах:  
- `app` — FastAPI-сервис  
- `db` — PostgreSQL 16

### 1. Клонируйте репозиторий

```bash
git clone <ваш-репозиторий>
cd mini-kanban-api
```

### 2. Запустите сервис

```bash
docker compose up --build
```

> 💡 В **GitHub Codespaces** или на Linux/macOS команда работает «из коробки».  
> На Windows убедитесь, что Docker Desktop запущен и WSL 2 настроен.

### 3. Доступ к API

- **API**: `http://localhost:8000`  
- **Swagger UI**: [http://localhost:8000/docs](http://localhost:8000/docs)  
- **Эндпоинт здоровья**: `GET /health` → возвращает `{"status":"OK"}` при успешном подключении к БД

---

## 🔑 Аутентификация

Все эндпоинты защищены API-ключом через заголовок:

```
x-api-key: test_authorization_key
```

Ключ задаётся в `docker-compose.yml` через переменную окружения `API_KEY`.

---

## 🗄️ База данных

- **Тип**: PostgreSQL 16 (в контейнере)
- **Схема**: автоматически создаются таблицы `boards`, `lists`, `cards`
- **Хранение**: данные сохраняются между перезапусками благодаря Docker volume `kanban_dbdata`
- **Конфигурация**: все параметры подключения задаются через переменные окружения:
  - `DB_HOST=db`
  - `DB_PORT=5432`
  - `DB_USER=kanbanuser`
  - `DB_PASSWORD=kanbanpass`
  - `DB_NAME=kanbandb`

---

## 📡 Эндпоинты

Структура API **полностью совместима** с оригинальной версией (ранее использовался `data.json`).

### Доски (Boards)

| Метод | Путь | Описание |
|-------|------|----------|
| `POST` | `/boards` | Создать новую доску |
| `GET`  | `/boards` | Получить все доски |

### Списки (Lists)

| Метод | Путь | Описание |
|-------|------|----------|
| `POST` | `/lists` | Создать новый список (колонку) |
| `GET`  | `/boards/{board_id}/lists` | Получить списки по ID доски |

### Карточки (Cards)

| Метод | Путь | Описание |
|-------|------|----------|
| `POST` | `/cards` | Создать новую карточку |
| `GET`  | `/cards` | Получить все карточки (с фильтрацией по `status` и `assignee`) |
| `GET`  | `/cards/{card_id}` | Получить карточку по ID |
| `PUT`  | `/cards/{card_id}` | Обновить карточку |
| `DELETE` | `/cards/{card_id}` | Удалить карточку |
| `POST` | `/cards/{card_id}/move` | Переместить карточку в другой список (требуется `target_list_id` в теле запроса) |

> 💡 Примечание: `status` по умолчанию — `"todo"`.

---

## 📥 Примеры запросов

### Проверка работоспособности
```bash
curl http://localhost:8000/health
# Ответ: {"status":"OK"}
```

### Создать доску
```bash
curl -X POST http://localhost:8000/boards \
  -H "x-api-key: my_secret_api_key_123" \
  -H "Content-Type: application/json" \
  -d '{"name": "Моя доска"}'
```

### Создать список
```bash
curl -X POST http://localhost:8000/lists \
  -H "x-api-key: my_secret_api_key_123" \
  -H "Content-Type: application/json" \
  -d '{"board_id": 1, "name": "В работе"}'
```

### Создать карточку
```bash
curl -X POST http://localhost:8000/cards \
  -H "x-api-key: my_secret_api_key_123" \
  -H "Content-Type: application/json" \
  -d '{
    "list_id": 1,
    "title": "Сделать README",
    "description": "Написать документацию для проекта",
    "assignee": "user@example.com",
    "status": "todo"
  }'
```

### Переместить карточку
```bash
curl -X POST http://localhost:8000/cards/1/move \
  -H "x-api-key: test_authorization_key" \
  -H "Content-Type: application/json" \
  -d '{"target_list_id": 2}'
```

---

## 📁 Структура проекта

```
mini-kanban-api/
├── app/
│   ├── __init__.py
│   ├── main.py          # Точка входа FastAPI
│   ├── models.py        # SQLAlchemy модели
│   ├── schemas.py       # Pydantic модели
│   ├── database.py      # Подключение к БД
│   └── repository.py    # Логика работы с БД
├── Dockerfile
├── docker-compose.yml
├── requirements.txt
└── README.md
```

---

## 📦 Зависимости

- **FastAPI** — веб-фреймворк
- **SQLAlchemy** — ORM для работы с PostgreSQL
- **psycopg2-binary** — драйвер PostgreSQL
- **uvicorn** — ASGI-сервер
- **python-dotenv** — загрузка переменных (для локальной разработки)

Все зависимости указаны в `requirements.txt`.

