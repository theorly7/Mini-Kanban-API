# Mini-Kanban-API

Простое REST API для управления досками, колонками (списками) и карточками в стиле Kanban.  

---

## 📦 Установка

1. Убедитесь, что у вас установлен Python 3.7+.
2. Установите зависимости:

```bash
pip install fastapi uvicorn
```

3. Сохраните код в файл, например `main.py`.

---

## ▶️ Запуск

Запустите сервер с помощью:

```bash
uvicorn main:app --reload
```

API будет доступно по адресу: [http://127.0.0.1:8000](http://127.0.0.1:8000)  
Документация Swagger UI: [http://127.0.0.1:8000/docs](http://127.0.0.1:8000/docs)

---

## 🔑 Аутентификация

Все эндпоинты защищены простым API-ключом через заголовок `X-API-Key`.

По умолчанию:  
```
X-API-Key: api_authorization_key
```

---

## 💾 Хранение данных

Данные сохраняются в файл `data.json` в корне проекта при завершении работы сервера (`atexit`).  
При запуске данные автоматически загружаются из этого файла (если он существует).

---

## 📡 Эндпоинты

### Доски (Boards)

| Метод | Путь | Описание |
|-------|------|----------|
| `POST` | `/boards` | Создать новую доску |
| `GET`  | `/boards` | Получить все доски |

### Списки (Lists)

| Метод | Путь | Описание |
|-------|------|----------|
| `POST` | `/lists` | Создать новый список (колонку) |
| `GET`  | `/boards/{board_id}/lists` | Получить списки по ID доски |

### Карточки (Cards)

| Метод | Путь | Описание |
|-------|------|----------|
| `POST` | `/cards` | Создать новую карточку |
| `GET`  | `/cards` | Получить все карточки (с фильтрацией по `status` и `assignee`) |
| `GET`  | `/cards/{card_id}` | Получить карточку по ID |
| `PUT`  | `/cards/{card_id}` | Обновить карточку |
| `DELETE` | `/cards/{card_id}` | Удалить карточку |
| `POST` | `/cards/{card_id}/move` | Переместить карточку в другой список (требуется `target_list_id` в теле запроса) |

> 💡 Примечание: `status` по умолчанию — `"todo"`.

---

## 📥 Примеры запросов

### Создать доску

```bash
curl -X POST http://127.0.0.1:8000/boards \
  -H "X-API-Key: api_authorization_key" \
  -H "Content-Type: application/json" \
  -d '{"name": "Моя доска"}'
```

### Создать список

```bash
curl -X POST http://127.0.0.1:8000/lists \
  -H "X-API-Key: api_authorization_key" \
  -H "Content-Type: application/json" \
  -d '{"board_id": 1, "name": "В работе"}'
```

### Создать карточку

```bash
curl -X POST http://127.0.0.1:8000/cards \
  -H "X-API-Key: api_authorization_key" \
  -H "Content-Type: application/json" \
  -d '{
    "list_id": 1,
    "title": "Сделать README",
    "description": "Написать документацию для проекта",
    "assignee": "user@example.com",
    "status": "todo"
  }'
```

### Переместить карточку

```bash
curl -X POST http://127.0.0.1:8000/cards/1/move \
  -H "X-API-Key: api_authorization_key" \
  -H "Content-Type: application/json" \
  -d '{"target_list_id": 2}'
```
